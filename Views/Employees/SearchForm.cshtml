@{
    ViewData["Title"] = "Search";
}

<h4>Search for Employees</h4>
<hr />
<p>Use employee names to search the database:</p>
<div class="row">
    <div class="col-md-4">
        <form asp-action="SearchFormResults">
            <div class="form-group">
                <label for="SearchInput" class="control-label"></label>
                <input name="SearchInput" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

